import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const assessmentAnswerImporterSelectors = importerSelectors(
  'assessmentAnswer.importer',
);

export default assessmentAnswerImporterSelectors;
