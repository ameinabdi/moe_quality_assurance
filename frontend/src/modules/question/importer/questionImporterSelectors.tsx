import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const questionImporterSelectors = importerSelectors(
  'question.importer',
);

export default questionImporterSelectors;
