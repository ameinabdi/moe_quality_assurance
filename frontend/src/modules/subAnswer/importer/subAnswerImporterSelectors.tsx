import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const subAnswerImporterSelectors = importerSelectors(
  'subAnswer.importer',
);

export default subAnswerImporterSelectors;
