import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const assessmentImporterSelectors = importerSelectors(
  'assessment.importer',
);

export default assessmentImporterSelectors;
