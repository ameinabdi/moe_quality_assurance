import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const districtImporterSelectors = importerSelectors(
  'district.importer',
);

export default districtImporterSelectors;
