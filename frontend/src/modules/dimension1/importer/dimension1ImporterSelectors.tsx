import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const dimension1ImporterSelectors = importerSelectors(
  'dimension1.importer',
);

export default dimension1ImporterSelectors;
