import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const questionSectionImporterSelectors = importerSelectors(
  'questionSection.importer',
);

export default questionSectionImporterSelectors;
